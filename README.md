# Element AI Vue Demo

这是一个基于 Vue 3 + Element Plus + Element AI Vue (Client) 和 NestJS (Server) 的全栈 AI 聊天应用演示。


### 方式一：一键启动 (推荐)

在根目录下运行以下命令，可同时启动前端和后端服务：

```bash
# 首次运行前安装所有依赖
npm run install:all

# 启动服务
npm run dev
```